const fs=require('fs');const html=fs.readFileSync('mcmp.html','utf8');const cheerio=require('cheerio');const ch=cheerio.load(html);const servers=[];ch('table.table tbody tr').each((i,el)=>{const name=ch(el).find('strong').first().text().trim();let ip=ch(el).find('button.copy-ip').attr('data-clipboard-text') || '';if(!ip)ip=ch(el).find('strong').parent().next().text().trim();const banner=ch(el).find('img.img-fluid').attr('src') || '';servers.push({name,ip,banner});});console.log(servers.slice(0, 5));
